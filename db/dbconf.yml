test:
    driver: postgres
    open: user=begizi dbname=droppio_dev sslmode=disable

development:
    driver: postgres
    open: user=begizi dbname=droppio_dev sslmode=disable

production:
    driver: postgres
    open: user=begizi dbname=droppio sslmode=disable

environment_variable_config:
    driver: $DB_DRIVER
    open: localhost
